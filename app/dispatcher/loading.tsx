import { RouteLoadingScreen } from "@/components/shared/route-loading-screen";

export default function Loading() {
  return <RouteLoadingScreen message="Loading dispatcher routes, requests, and tracking data." title="Opening dispatcher view" />;
}
